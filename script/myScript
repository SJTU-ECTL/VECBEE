sweep; eliminate -1
simplify -l
eliminate -1

sweep; eliminate -1
simplify -l
eliminate -1

resub -a

gkx -abt 30
resub -a; sweep
gcx -bt 30
resub -a; sweep

gkx -abt 10
resub -a; sweep
gcx -bt 10
resub -a; sweep

gkx -ab
resub -a; sweep
gcx -b
resub -a; sweep

eliminate 0
decomp -g *

eliminate -1; sweep

map -m 0
print_map_stats
